[33mcommit 7593a24b5fa0d35587e850a36bc559cb51a9f4eb[m
Author: Béné Geoffroy <bene.geoffroy@outlook.fr>
Date:   Thu Aug 21 14:21:42 2025 +0200

    Fix Express route fallback

[1mdiff --git a/server.js b/server.js[m
[1mindex a5816dc..790479e 100644[m
[1m--- a/server.js[m
[1m+++ b/server.js[m
[36m@@ -1,51 +1,27 @@[m
 import express from "express";[m
[31m-import fs from "fs";[m
 import path from "path";[m
 import { fileURLToPath } from "url";[m
 [m
[31m-// Pour avoir __dirname avec ES modules[m
[32m+[m[32mconst app = express();[m
[32m+[m[32mconst PORT = process.env.PORT || 3000;[m
[32m+[m
[32m+[m[32m// Correction pour __dirname avec ES modules[m
 const __filename = fileURLToPath(import.meta.url);[m
 const __dirname = path.dirname(__filename);[m
 [m
[31m-const app = express();[m
[31m-const PORT = process.env.PORT || 3000; // Render utilise PORT fourni par l'env[m
[31m-[m
[31m-// Middleware pour parser le JSON[m
[32m+[m[32m// Middleware pour parser les requêtes JSON et formulaire[m
 app.use(express.json());[m
[32m+[m[32mapp.use(express.urlencoded({ extended: true }));[m
 [m
[31m-// Servir les fichiers statiques depuis le dossier 'site'[m
[32m+[m[32m// Servir les fichiers statiques depuis le dossier "site"[m
 app.use(express.static(path.join(__dirname, "site")));[m
 [m
[31m-// Endpoint pour mettre à jour menu.json[m
[31m-app.post("/update-menu", (req, res) => {[m
[31m-  const newMenu = req.body;[m
[31m-[m
[31m-  fs.writeFile([m
[31m-    path.join(__dirname, "menu.json"),[m
[31m-    JSON.stringify(newMenu, null, 2),[m
[31m-    (err) => {[m
[31m-      if (err) {[m
[31m-        console.error("❌ Erreur d'écriture:", err);[m
[31m-        return res.status(500).json({ message: "Erreur lors de la sauvegarde." });[m
[31m-      }[m
[31m-      console.log("✅ menu.json mis à jour");[m
[31m-      res.json({ message: "Menu mis à jour avec succès !" });[m
[31m-    }[m
[31m-  );[m
[31m-});[m
[31m-[m
[31m-// Sécurité basique (exemple : future authentification admin)[m
[31m-app.use("/admin", (req, res, next) => {[m
[31m-  // À améliorer plus tard (JWT ou sessions)[m
[31m-  next();[m
[31m-});[m
[31m-[m
[31m-// Fallback : renvoyer index.html pour toute route non trouvée[m
[31m-app.get("*", (req, res) => {[m
[32m+[m[32m// Fallback pour toutes les autres routes (⚠️ corrigé ici)[m
[32m+[m[32mapp.get("/*", (req, res) => {[m
   res.sendFile(path.join(__dirname, "site", "index.html"));[m
 });[m
 [m
[31m-// Démarrage du serveur[m
[32m+[m[32m// Lancer le serveur[m
 app.listen(PORT, () => {[m
[31m-  console.log(`🚀 Serveur lancé sur http://localhost:${PORT}`);[m
[32m+[m[32m  console.log(`🚀 Serveur en ligne sur le port ${PORT}`);[m
 });[m
